using UnityEngine;
using System.Collections;

public class UI_Ctrl : MonoBehaviour {

	//Use this script to hold references to all UI elements. 
	//That way we don't need to look for them in the hierarchy and can disable them there f.x.

	public GameObject GameOver;

	public GameObject healthBar;


}
